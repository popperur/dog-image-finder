import styled from "styled-components";
import { Card } from "antd";

export const DogImagesContainer = styled.div`
  padding: 40px;
`;

export const DogImageCard = styled(Card)`
  .ant-card-body {
    padding: 8px;
  }
`;
